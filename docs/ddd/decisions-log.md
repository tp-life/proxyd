# DDD 决策记录

## 2026-09-04 — 独立远程桌面上下文

- 已确认“远程桌面”与“远程连接”分成两个 Web 页面；前者管理 RDP/VNC，后者保留 tailcat、SSH 与通用转发。
- 已确认桌面服务端口可配置，并分别展示操作系统服务监听状态与 `remote.serve` 开放状态。
- 已确认客户端连接档案持久化，但只引用远端名称，不保存密码或第二份 token。
- 已确认 Web 临时会话运行在 proxyd 守护进程内，使用单清扫协程按首次连接宽限、空闲和最长寿命回收。
- 已确认纯 CLI `proxyd desk` 保留，用于必须从当前登录用户会话直接启动 GUI 的场景。

## 2026-09-02 — 控制台体验与代理运行控制 Phase 1

- 已确认端口映射开关只控制一对一节点 listener，并保留稳定端口分配。
- 已确认订阅支持启停与编辑；停用保留配置和缓存，重命名原子更新策略组引用。
- 已确认本地自定义规则支持编辑与排序，远程规则源内容保持只读。
- 已确认配置导入使用“预检—摘要确认—原子写盘”流程，且继续要求重启生效。
- 订阅刷新周期继续保持全局配置，不在本轮引入每订阅独立调度器。
- proxyd 保持守护进程与 Web 控制台定位，不引入托盘、窗口热键、CSS 注入、WebDAV 或内核切换。
